terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"          # use latest stable in 2026
    }
  }
}

provider "aws" {
  region = "ap-south-1"          # change to your region (e.g. Mumbai)
}